# ORPHANED FIXTURE — Not referenced by any include_str! in Rust source.
# Produced by: `git diff` in the rtco project root.
# Intended for: crates/rtco-cli/src/cmds/git/git.rs (module: git)
# Created: 2026-06-16
# Note: The `pipe_cmd.rs` module has a `git_diff_wrapper` function but does NOT use this fixture.
#
diff --git a/src/core/filter.rs b/src/core/filter.rs
index a1b2c3d..e4f5a6b 100644
--- a/src/core/filter.rs
+++ b/src/core/filter.rs
@@ -42,6 +42,15 @@ lazy_static! {
     static ref ANSI_RE: Regex = Regex::new(r"\x1b\[[0-9;]*m").unwrap();
 }
