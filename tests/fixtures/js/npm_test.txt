# ORPHANED FIXTURE — Not referenced by any include_str! in Rust source.
# Produced by: `npm test` in the rtco project root (simulated Jest run from package.json scripts.test).
# Intended for: crates/rtco-cli/src/cmds/js/npm_cmd.rs (module: js/npm)
# Created: 2026-06-16
# Note: npm_test.txt is output of `npm test` which delegates to Jest.
#
> rtco@0.1.0 test /data/projects/rust_token_cost_optimizer
> jest --passWithNoTests

 PASS   tests/common/mod.test.ts
  TokenCounter
    ✓ counts tokens for empty string (1 ms)
    ✓ counts tokens for single word (1 ms)
    ✓ counts tokens for multiple words
