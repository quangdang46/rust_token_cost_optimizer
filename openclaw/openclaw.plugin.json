{
  "id": "rtco-rewrite",
  "name": "RTCO Token Optimizer",
  "version": "1.0.0",
  "description": "Transparently rewrites shell commands to their RTCO equivalents for 60-90% LLM token savings",
  "homepage": "https://github.com/rtco-ai/rtco",
  "license": "Apache-2.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable automatic command rewriting to RTCO equivalents"
      },
      "verbose": {
        "type": "boolean",
        "default": false,
        "description": "Log rewrite decisions to console for debugging"
      }
    }
  },
  "uiHints": {
    "enabled": { "label": "Enable RTCO rewriting" },
    "verbose": { "label": "Verbose logging" }
  }
}
